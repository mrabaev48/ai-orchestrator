# 1601 — Открытие AgentRoleName → RoleKey

**Фаза:** P2 · **Оценка:** M · **Зависит от:** —
**Файлы:** `packages/core/src/roles.ts` и все потребители `Record<AgentRoleName, …>`

## Контекст
`AgentRoleName` — закрытый union из 12 значений (`packages/core/src/roles.ts:3`). Он используется
как ключ записей, в частности шаблонов промптов (`packages/prompts/src/prompt-pipeline.ts:16-29`).
Пока union закрыт, добавить агента без пересборки невозможно.

## Задача
Чисто механическая правка: `RoleKey = string & { __brand }`, константа `BUILTIN_ROLE_KEYS`,
все словари по ролям — с явной обработкой отсутствующего ключа.

## Критерии приёмки
- [ ] Поведение не изменилось: все существующие тесты зелёные без правок ожиданий.
- [ ] Отсутствие шаблона/схемы для роли даёт понятную ошибку, а не `undefined` в промпте.
- [ ] Отдельный коммит, не смешанный с функциональностью.

## Тесты
- Регрессия всех role-тестов + новый тест «неизвестная роль → внятная ошибка».
