# 1903 — Вынос RBAC/ABAC в общий модуль

**Фаза:** P3 · **Оценка:** M · **Зависит от:** 1902
**Файлы:** `packages/application/src/authorization/*`, `apps/control-plane/src/authz/rbac-abac.ts`

## Задача
Одна реализация авторизации для CLI и HTTP: сейчас логика живёт в приложении control-plane
и недоступна API.

## Критерии приёмки
- [ ] Матрица «действие × роль × атрибуты» описана один раз и используется обоими входами.
- [ ] Поведение CLI не изменилось (`tests/rbac-abac-control-plane.test.ts` зелёный).
- [ ] Решения авторизации логируются одинаково для CLI и API.

## Тесты
- Юнит матрицы; E2E: одно и то же действие через CLI и API даёт одинаковое решение.
