# 2404 — Ревизия зависимостей

**Фаза:** P4 · **Оценка:** S · **Зависит от:** 1503
**Файлы:** `package.json`, `packages/*/package.json`

## Задача
Привести зависимости в соответствие с фактическим использованием.

## Объём
- `@modelcontextprotocol/sdk` — переехал из корня в `packages/capabilities` и реально используется
  (до группы 14 он числился в зависимостях без единого использования).
- Проверить дублирование runtime-зависимостей между корнем и пакетами.
- Тестовые зависимости (`supertest`, `@nestjs/testing`) — в devDependencies.

## Критерии приёмки
- [ ] Каждая зависимость корня обоснована использованием.
- [ ] `pnpm run boundaries` и сборка зелёные.
- [ ] В CI добавлена проверка неиспользуемых зависимостей.

## Тесты
- CI-шаг проверки; полная сборка.
