# 0408 — Дополнительные облачные адаптеры (Google, Azure OpenAI, Bedrock)

**Фаза:** P0/опционально · **Оценка:** L · **Зависит от:** 0401, 0402
**Файлы:** `packages/llm/src/adapters/{google,azure-openai,bedrock}.ts`

## Задача
Расширить покрытие провайдеров. Приоритет ниже локальных: делать по фактическому спросу.

## Объём
- Azure OpenAI — переиспользовать OpenAI-адаптер с другим URL/аутентификацией (deployment name вместо model).
- Google Gemini — свой формат `contents` / `generationConfig.responseSchema`.
- AWS Bedrock — SigV4-подпись, разные семейства моделей за одним API.

## Критерии приёмки
- [ ] Каждый адаптер проходит общий контрактный набор (0409-стиль: одинаковые сценарии для всех адаптеров).
- [ ] Отсутствие адаптера не влияет на сборку (ленивая регистрация).

## Тесты
- Контрактный набор на фикстурах для каждого адаптера.

## Не входит
- Автоматический выбор региона, VPC-эндпоинты.
