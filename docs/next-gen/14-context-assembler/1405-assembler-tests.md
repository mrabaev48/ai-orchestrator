# 1405 — Тестовый набор сборщика контекста

**Фаза:** P2 · **Оценка:** M · **Зависит от:** 1402, 1403
**Файлы:** `tests/context/assembler.test.ts` (новый)

## Проверяемое
- Соблюдение бюджета для окон 8k / 32k / 200k.
- Стабильность кэшируемого префикса между шагами одного прогона.
- Присутствие enforced-правил при любом дефиците.
- Отсутствие секретов в собранном контексте (через `redactSecrets`).
- Детерминизм: одинаковый вход → байт-в-байт одинаковый выход.

## Критерии приёмки
- [ ] Все пять свойств покрыты и зелёные.
- [ ] Тест использует реальные фикстуры состояния, а не синтетические заглушки.

## Тесты
- Сам тест является артефактом.
